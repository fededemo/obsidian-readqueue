---
description: Entrypoint de Cursor, gate local y vault. Always on.
alwaysApply: true
---

# Cursor

Agentes en `.cursor/agents/` (`model: inherit`). Mapa: `AGENTS.md`.

Gate de este repo, en esta máquina, antes de mergear. `gh run list` no es señal.

Vault: `/Users/federico/fedenotes`. Escritura solo en `vibecoder/<Proyecto>/Observaciones.md`. No editar texto de Fede ni `status` de un artículo.
